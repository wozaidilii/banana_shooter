{
  "scripts": {
    "build": "esbuild game.js --bundle --format=iife --global-name=BananaStrike --outfile=game.bundle.js && node scripts/prepare-vercel-output.mjs"
  },
  "dependencies": {
    "three": "0.184.0"
  },
  "devDependencies": {
    "esbuild": "0.28.1"
  }
}
